<%= bootstrap_form_for(@characteristic) do |f| %>
   <%= f.alert_message "Please fix the errors below." %>

  <div class="form-inputs">
  
    <%= f.input :question, as: :ckeditor %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
