
const stripe = '<%= Rails.application.credentials.dig(:stripe, Rails.evn.to_sym,
 :secret_key)%>';

stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>'
}).then (function (result) {
    console.log(result.error.message);
});
