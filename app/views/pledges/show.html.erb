<script type="text/javascript" src="https://js.stripe.com/v2/"></script>

<div class="mt50"></div>
  <div class="row">    
    <div class="col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
      <div class="pledge-text">
        <p><strong> You have elected the <span id="plan"><em><%= @pledge.plan %></em></span> pay plan for your annual pledge of <%= number_to_currency(@pledge.amount) %>. That would be <%= @pledge.divisor %> payments of <span id="payThis"><%= number_to_currency((@pledge.pay_this.to_f)/100) %></span>.</strong></p>
        <div class="mt30"></div>
          <p>If these numbers are not correct, click "edit pledge" to make changes. If correct, click "checkout." You will be taken to a secure website that handles credit and debit card payments for our church.</p>
        <div class="mt30"></div>
        <p><strong>
          If your circumstances change or for any other reason you wish to edit or remove these settings you may do so at any time.</strong></p>
        <div class="mt30"></div>
          <%= link_to "Edit pledge", edit_pledge_path(@pledge), class: "btn btn-primary btn-lg floatleft" %> 
 
     <%= form_tag pledge_charges_path, id: "payment-form" do %>
      <span class="payment-errors"></span>

      <div class="form-row">
        <label>
          <span>Card Number</span>
          <input value="4242 4242 4242 4242" type="text" size="20" data-stripe="number">
        </label>
      </div>

      <div class="form-row">
        <label>
          <span>Expiration (MM/YY)</span>
          <input value = "12" type="text" size="2" data-stripe="exp_month">
        </label>
        <span> / </span>
        <input value= "19" type="text" size="2" data-stripe="exp_year">
      </div>

      <div class="form-row">
        <label>
          <span>CVC</span>
          <input value = "333" type="text" size="4" data-stripe="cvc">
        </label>
      </div>


      <button type="submit" class="btn btn-lg btn-primary"> Submit pledge payment </button>
      <% end %>
    </div>
  </div>
</div>
