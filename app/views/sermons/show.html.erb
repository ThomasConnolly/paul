  
<div class="sermon-page">
  <h1><%= @sermon.title %></h1>
    <p><%= @sermon.cites %></p>
  <%= render 'serbox' %>
  
  <p class="sermon-text">
    <%= raw @sermon.sermon_body %>
  </p><br>
    
    <% if current_user.admin? %>
    <%= link_to 'Edit', edit_sermon_path(@sermon) %> | 
    <% end %>
</div>
 