<%= simple_form_for :post, url: posts_path, html: { multipart: true } do |f| %>
  
    <%= f.input :content, label: "Post your status" %>
    <%= f.attachment_field :picture %>
    
    <%= f.button :submit, class: 'btn btn-success' %>
 <br>
  <% end %>
  
<br>
<%= link_to '<<Back', posts_path %>
