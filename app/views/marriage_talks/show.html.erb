<div class="mt50"></div> 
<div class="row">
  <div class="col col-md-10 col-md-offset-1 col-sm-12">
  <p id="notice"><%= notice %></p>

<h1><%= @marriage_talk.title %></h1>
<p>
  <strong>Delivered on <%= @marriage_talk.delivered_on.strftime("%b %d, %Y") %></strong>
  <div class="sermon">
  <p>
  <%= raw @marriage_talk.body %>
  </p>
  </div>
<% if current_user && current_user.has_role?(:admin) %>
<%= link_to 'Edit', edit_marriage_talk_path(@marriage_talk) %> 
<% end %>
<%= link_to 'Back', marriage_talks_path %>
