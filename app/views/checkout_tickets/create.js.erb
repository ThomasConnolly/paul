const stripe = Stripe('<%= Rails.application.credentials.dig(:stripe, :production, :publishable_key %>');
stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>'
}).then (function (result) {
    console.log(result.error.message);
});
