<h3>Comments</h3>
<% commentable.comments.each do |comment| %>
  <div class="well" id="comments">
    <p><%= comment.body %><br>
    <%= comment.user.full_name.titleize %>

    <span class="time-ago"><%= time_ago_in_words(comment.created_at) %> ago.</span></p>
  </div>

 <!--   <%# if comment.user == current_user %>
    <span class="edit-delete-line"><%#= link_to post_comment_path(,comment), method: :delete, data: { confirm: "Are you sure?" }, remote: true do %> "Delete"</span>
</p>

    <%# end %>         
  <%# end %> -->
<% end %>
