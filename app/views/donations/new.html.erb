
<h1>Your donation to St. Paul's </h1>
<div class="mt-3"></div>
<% if user_signed_in? %>
  <div class="text-center mt-3 col-md-4 offset-md-4">   
    <h2>Donate now,</h2>
    <%= render 'form' %>
  </div>
  
<h2>Or set up automatically recurring deductions using your credit or debit card</h2>    
  <div class="text-center mt-3 col-md-4 offset-md-4">
    <div class="card">
      <div class="card-body">
      <h4 class="card-title">Recurring payment</h4>
      <p class="card-text">
      A convenient and safe way to keep your offering pledge current.</p>
      <%= link_to "Set this up", new_pledge_path, class: "btn btn-primary" %>
      </div>
    </div>
  
<% else %>
    <p><strong>Start by signing in, or if you have not registered, by registering. If you have been registered but forgot your password, click <em>Sign in</em> then click on "forgot my password" and you will get an email with instructions about how to set a new password. Click one:</p>
  
    <div class="d-flex text-center justify-content-center" width="400px">
      <h2 class="mb-5">
        <%= link_to "Register", new_user_registration_path %> &nbsp; or &nbsp; 
        <%= link_to "Sign-in", new_user_session_path %></h2>
    </div>
  <% end %>
</div>
  <div class="mt-5 mb-5"></div>
