<h3>Comments</h3>
<% commentable.comments.each do |comment| %>
  <div class="well" id="comments">
    <p><%= comment.body %><br>
      <div class="comments-user">
    <%= comment.user.full_name.titleize %>
      <span class="time-ago">
      <%= time_ago_in_words(comment.created_at) %> ago.
      </span></p>
      </div>
  </div>

 <!-- <%# if comment.user == current_user %>
    <span class="edit-delete-line"><p><small><%#= link_to "Delete", post_comment_path(comment), method: :delete, data: { confirm: "Are you sure?" } %> </span>
    </small></p> -->

    <%# end %>         
  <% end %> 

