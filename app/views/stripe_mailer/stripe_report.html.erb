<!DOCTYPE html>
<html>
  <head>
    <style>
      table {
        width: 100%;
        border-collapse: collapse;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      th {
        background-color: #f2f2f2;
      }
    </style>
  </head>
  
  <body>
    <h1>New Stripe Report</h1>
    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Name</th>
          <th>Amount</th>
          <th>Stripe Fee</th>
          <th>Net</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= @stripe_report.created_at.strftime("%b %-d, %Y") %></td>
          <td><%= @stripe_report.donor_name %></td>
          <td><%= number_to_currency(@stripe_report.amount / 100.0) %></td>
          <td><%= number_to_currency(@stripe_report.stripe_fee / 100.0) %></td>
          <td><%= number_to_currency(@stripe_report.net / 100.0) %></td>
        </tr>
      </tbody>
    </table>
  </body>
</html>
