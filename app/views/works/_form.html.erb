<div class="form-inputs"> 
  <%= simple_form_for @work do |f| %>
    <%= f.input :my_act, label: false, placeholder: "God's grace inspires me to...", autofocus: true %>
        <div class="honey"> <%= f.input :honey %> </div>
  <%= f.button :submit, "Submit", class: "btn btn-primary btn-sm mt-30" %>
</div>
  <% end %>
