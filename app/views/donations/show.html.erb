<h1><%= @donation.user.username.titleize %>'s donation</h1>

<p> <strong>Amount:</strong>
  <%= formatted_amount(@donation.amount) %>
</p>

<%= link_to "Checkout", donation_payment_path(@donation), class: "btn btn-primary" %>


<div class="mt-5 mb-5"></div>

<% if current_user == @donation.user %>
<%= link_to 'Edit', edit_donation_path(@donation) %> |
<% end %>
<%= link_to 'Back', donations_path %>
