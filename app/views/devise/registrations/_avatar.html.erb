<h3>Add or change your profile picture</h3>
<%=simple_form_for(current_user, html: { :multipart => true }) do |f| %>
  <%= f.input :avatar, as: :attachinary %>
  <%= f.button :submit, "Update photo", class: "btn btn-primary" %>
<% end %>  

