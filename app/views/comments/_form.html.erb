  <%= simple_form_for [@post, @post.comments.build], remote: true do |f| %>
    <%= f.hidden_field :parent_id %>
    <%= f.input :body, placeholder: 'Type your comment here', 
    input_html: { rows: 2 }, label: false %>
    <%= f.submit %>
  <% end %>
  