  <%= bootstrap_form_with model: @donation do |f| %> 
    <%= f.number_field :dollars, prepend: "$", append: ".00", placeholder: "whole dollars", hide_label: true, width: '200px' %>
      <%= f.submit "Set up donation", class: "btn btn-success btn-lg mb-5" %>
<% end %>