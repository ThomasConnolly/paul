<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
<h1>Here's the post</h1>
  <%= @post.content %>
  <%= @post.user.full_name.titleize %>
<br>
<% @post.comments.each do |comment| %>
<%= comment.body %><br>
<span class="time-ago">Comment made <%= time_ago_in_words(comment.created_at) %> ago. 
      </span></p>

<% end %>
<br>
<%= link_to "<< Back to Posts", posts_path %>
